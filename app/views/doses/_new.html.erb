<%= simple_form_for([cocktail, dose]) do |f| %>
  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <!-- association is giving some input that orignates from a different model -->
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
